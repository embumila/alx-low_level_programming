

#include "main.h"

/**
 * _islower - Examine for lower case character
 *
 * Return: 1 if lowercase character identified else 0
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
